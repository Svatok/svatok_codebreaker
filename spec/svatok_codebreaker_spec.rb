require "spec_helper"

describe SvatokCodebreaker do
  it "has a version number" do
    expect(SvatokCodebreaker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
